apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app.kubernetes.io/instance: ai-demo-app
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: ai-demo-app
  name: ai-demo-app
spec:
  port:
    targetPort: 8080
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: ai-demo-app
    weight: 100
